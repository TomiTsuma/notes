# ConnectionForm

Nextcloud connection settings: status, server URL, username, app password and the papers / sync folder pickers.

## Props

`status` connected|connecting|failed|idle · `url` · `username` · `papersPath` · `syncPath`.

## Usage

- Shown inside a Modal from the sidebar footer, and inline in the Library when disconnected.
