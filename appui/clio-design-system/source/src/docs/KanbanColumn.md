# KanbanColumn

A status column: StatusPill header with count, add/⋯ actions, the task stack and a dashed "New task" button.

## Props

`status` · `count` · `over` (drop target) · `onAdd` · children.

## Usage

- Four columns, always in status order.
