# Menu

Context and overflow menu (right-click on files and folders, ⋯ buttons). Also exports `Tooltip`.

## Props

`items` [{icon,label,shortcut,danger} | "-" | {heading,label}]. Tooltip: children, `shortcut`.

## Usage

- Destructive items go last, after a divider.
