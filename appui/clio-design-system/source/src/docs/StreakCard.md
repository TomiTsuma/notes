# StreakCard

The streak block: flame, day count, a 7-day bubble row and totals for tasks done and notes logged.

## Props

`days` · `week` (7 × 0/1) · `todayIndex` · `tasksDone` · `notesLogged`.

## Usage

- Data comes from `userStreak`; done days use `sage-solid` with a check mark.
