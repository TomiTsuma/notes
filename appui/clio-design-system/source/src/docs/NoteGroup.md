# NoteGroup

Pastel container that groups note cards under a heading — the signature block of Clio’s notes home.

## Props

`title` · `tone` (tint) · `onAdd` · children (NoteCards).

## Usage

- Tone follows the project or folder colour.
- Cards inside stay white (`surface-raised`); the wash is the frame, not the card.
