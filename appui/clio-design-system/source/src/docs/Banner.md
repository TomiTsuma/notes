# Banner

Inline status message: info, AI, success or danger (sync error).

## Props

`tone` info|ai|success|danger · `title` · children · `action`.

## Usage

- The sync error banner replaces the red full-width bar.
