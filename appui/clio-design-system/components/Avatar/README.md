# Avatar

Round initials or photo avatar for the signed-in user and, later, collaborators.

## Props

`name` · `src` · `size` (px) · `tone`.

## Usage

- Clio is single-user today: avatars appear in the sidebar user card only. Do not decorate cards with avatar stacks.
