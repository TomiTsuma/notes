# LibraryPage

Nextcloud Library folder view: path, creation actions (arXiv, upload, notepad, sticky, subfolder), folders, papers, notepads and sticky board.

Composed only from Clio components and tokens; open the preview and use the theme toggle in the top bar to see both themes.
