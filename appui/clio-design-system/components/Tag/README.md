# Tag

Small tinted chip for tags, categories and short metadata.

## Props

`tone` rose|sky|sage|amber|lilac|stone · `onRemove` (shows ×) · `outline` · `icon`.

## Usage

- User tags are written `#name` in lowercase.
- One to two words. Emoji are allowed only when the user typed them into their own tag.
