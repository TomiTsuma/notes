# Composer

The AI input card: @context chips (the active paper by default), textarea, attach, model label and send/stop.

## Props

`context` string[] · `placeholder` · `model` · `busy` (send becomes stop).

## Usage

- The active document is always the first context chip.
