# DashboardPage

Home: greeting, quick actions, streak, today’s agenda, daily tasks, active projects and recent notebooks. Every block of the old HomeDashboard is kept; the rotating wallpaper becomes the optional `canvas` backdrop behind the window.

Composed only from Clio components and tokens; open the preview and use the theme toggle in the top bar to see both themes.
