# AgendaItem

One row of the Dashboard "Today’s agenda": mono time, tinted event with a colour rail.

## Props

`time` · `title` · `detail` · `tone` · `done`.

## Usage

- Tone = the event’s project colour.
