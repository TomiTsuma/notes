# FolderCard

Folder tile for the Nextcloud Library (FolderFloat replacement) with a tab, tint, count and page peek.

## Props

`name` · `count` (string) · `tone` · `peek` (0–3 paper slips).

## Usage

- Tone is stone for system folders (Inbox), a tint for user folders.
