# SmartNote

One smart-note section (Summary, Datasets, …) with idle / loading / done / error states and generate, retry, regenerate.

## Props

`title` · `status` idle|loading|done|error · `open` · children (the generated text). Exports `SMART_PROMPTS`.

## Usage

- Keeps the nine prompts of the old RightPanel.
