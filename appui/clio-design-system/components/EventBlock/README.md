# EventBlock

A calendar event: tinted block with a colour rail, title and mono time; `compact` for month cells.

## Props

`title` · `time` · `tone` · `done` · `compact` · `style` (position in the grid).

## Usage

- Clicking opens the event bottom sheet (use `.cl-sheet`) with Edit / Delete.
